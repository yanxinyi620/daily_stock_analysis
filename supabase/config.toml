# Local-only defaults. Configure hosted Auth separately in the Supabase dashboard.
project_id = "daily-stock-analysis"

[auth]
enable_signup = false
enable_anonymous_sign_ins = false
minimum_password_length = 12

[auth.email]
enable_signup = false
