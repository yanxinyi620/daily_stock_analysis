# 云端分析记录表

用户已确认将“分析报告”和“最近发布”合并。沿用现有 React/Vite 页面和风格，不改变数据库写入、分析引擎或用户隔离。

- 以用户的发布记录分页，关联同一 ID 的报告和执行结果，避免重复行；保存失败或尚无报告的条目仍展示。
- 展示报告名称、来源、分析结果、保存状态、时间及查看/下载。保存成功不等于分析完整；历史缺失信息显示“未记录”。
- 表格使用页面全宽，窄屏允许表格区域横向滚动，不撑宽页面。
- 保留未完成保存的轮询、分页、登录切换清理、错误重试与私有附件下载；取消独立“最近发布”区域。
- 验证数据关联、失败记录、历史结果、分页/刷新、会话切换和下载；运行 Web 测试、lint/build。截图放在仓库外。
- 验证后更新正式部署及文档，不更改 Supabase 数据或旧迁移。
